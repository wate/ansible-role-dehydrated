# Additional config
{% for cfg_name, cfg_value in dehydrated_cfg.items() -%}
{{ cfg_name }}="{{ cfg_value }}"
{% endfor %}
HOOK="/etc/dehydrated/hook.sh"
# Ensure pipx-installed binaries (e.g., dns-lexicon) are discoverable in cron environment
export PATH="/usr/local/bin:${PATH}"
